#!/bin/sh

#========================================================================
# GNU sedラッパースクリプト
#========================================================================

#=========================================================================
# sedコマンドパス
#=========================================================================
selfpath=$(realpath $0)
systemcmd=$(which -a gsed | grep -v "$selfpath" | head -1)

#=========================================================================
# プラットフォームごとに処理を分岐
#=========================================================================
case "$(uname)" in
MSYS*)
	exec /usr/bin/sed "$@"
	;;
*)
	exec "$systemcmd" "$@"
	;;
esac
